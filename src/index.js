(function() {
  const directives = [{}];

  const smallAngular = {
    directive() {
      // do smth
    },
    compile(node) {
      // do smth
    },
    bootstrap(node) {
      // do smth
    }
  };
  window.smallAngular = smallAngular;
})();